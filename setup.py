from setuptools import setup, find_packages


setup(
    name='novelty',
    version='1.0.0',
    install_requires=[],
    packages=find_packages(),
    entry_points={
        'console_scripts': [
        ]
    }
)
